#ifndef FLIR_TYPES_H
#define FLIR_TYPES_H

// FLIR types are defined via TableGen TypeDef (`FlirTypeDefs.td`) and included
// via `flir/FlirDialect.h` (which includes `flir/FlirTypes.h.inc`).
//
// This header is kept to preserve include paths in downstream code.
#include "flir/FlirDialect.h"

#endif // FLIR_TYPES_H

